/** DealList 对象渲染（Phase 2 实装）。 */
export default function DealListView({ payload }: { payload: unknown }) {
  void payload;
  return (
    <div className="rounded-lg border border-dashed border-slate-300 p-3 text-sm text-slate-400">
      候选项目池 —— Phase 2 实装
    </div>
  );
}
