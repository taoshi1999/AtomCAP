/**
 * 项目工作台（Phase 2 起实装）。
 * 左侧：项目列表（deal 状态机）；右侧：Pre-DD Agent 会话 + checklist + DD 报告。
 */
export default function WorkspacePage() {
  return (
    <div className="flex h-screen items-center justify-center text-slate-400">
      项目工作台 —— Phase 2 接入项目列表，Phase 3 接入 Pre-DD Agent
    </div>
  );
}
